# 自定义技能库 — 悦指间美甲小程序

## preview-app

启动微信开发者工具预览美甲小程序：

1. 确认项目在微信开发者工具中已打开（路径：`d:\wechat_project\nail-salon-miniapp`）
2. 在开发者工具中点击「**预览**」→ 生成二维码
3. 用手机微信扫码即可体验小程序

### 注意

- 预览模式下，云开发功能不可用（需要在微信后台开通云开发后，真机预览才会走云数据）
- 当前未开通云开发时，所有数据自动使用本地 Mock 数据
- 模拟器中正常显示的功能，真机上基本一致

---

## upload-app

将小程序代码上传到微信后台，准备提交审核：

### 上传流程

1. **确认项目完整**：检查所有页面在模拟器中运行正常
2. **点击上传**：微信开发者工具右上角 →「上传」→ 填写版本号（如 `v1.0.0`）
3. **填写项目备注**：简要描述本次更新内容
4. **上传完成**：登录 [mp.weixin.qq.com](https://mp.weixin.qq.com) →「管理」→「版本管理」查看

### 上传后的步骤

| 步骤 | 操作 | 耗时 |
|------|------|------|
| 1. 提交审核 | 开发版本 → 点击「提交审核」→ 填写类目和标签 | — |
| 2. 等待审核 | 微信官方审核（检查功能是否正常、内容是否合规） | 1~7 个工作日 |
| 3. 发布上线 | 审核通过 → 点击「发布」→ 用户即可搜索到你的小程序 | — |

### 审核注意事项

- 类目选「生活服务 > 美容美甲」
- 小程序名称必须与备案信息一致
- 确保没有测试数据（如"test""测试"等占位文字）
- 首页不能是空白页

---

## comments-check

对项目源码进行三维度注释质量检查——覆盖率、准确性、小白友好度，并给出改进建议和最终评分。

### 什么是注释检查？

> 就像老师批改作文——不仅看字数够不够（覆盖率），还看内容对不对（准确性），更看能不能让小学生读懂（小白视角）。
> 注释检查 = 逐行审查代码注释，确保每一段核心逻辑都有清晰的中文解释。

### 使用方式

| 命令 | 功能 |
|------|------|
| `/comments-check` | 扫描全部源码，输出完整检查报告 |
| `/comments-check 文件路径` | 只检查指定文件 |
| `/comments-check 关键词` | 检查文件名包含关键词的文件 |
| `/comments-check 目录路径` | 检查指定目录下的所有源码 |

### 检查排除规则

检查时**跳过**以下内容：
- `scripts/` — 工具脚本不需要注释检查
- `utils/data.js` — 纯数据文件
- `cloudfunctions/` — 云函数单独检查
- `node_modules/` — 第三方依赖

---

### 三维度检查标准

#### 维度一：注释覆盖率（权重 40%）

> 目标：**10 行代码中，至少 3 行是注释**（约 30% 注释率）

**统计方法：**

| 行类型 | 判定标准 | 示例 |
|--------|----------|------|
| 注释行 | `//` 开头、`/* */` 包裹、`/** */` JSDoc | `// 计算积分余额` |
| 代码行 | 包含实际逻辑/声明的行 | `const balance = total - used;` |
| 空行 | 完全空白或只有空格 | （不计入，单独统计） |
| 花括号行 | 只有 `{` 或 `}` | （不计入） |

**评分标准：**

| 注释率 | 评分 | 等级 |
|--------|:----:|:----:|
| ≥ 30% | ⭐⭐⭐ | 优秀 |
| 20% ~ 29% | ⭐⭐ | 良好 |
| 10% ~ 19% | ⭐ | 需改进 |
| < 10% | ❌ | 不合格 |

**重点检查对象：**
- 每个 `Page({})` 中的自定义方法必须有注释说明用途
- 每个 `utils/cloud.js` 导出的函数必须有 JSDoc
- 复杂逻辑块必须有行内注释（解释"为什么这样做"）
- 每个页面的 JS 文件顶部必须有模块说明

#### 维度二：注释内容准确性（权重 35%）

> 注释必须准确描述代码的实际行为，不能"说一套做一套"

**常见错误类型：**

| 类型 | 示例 | 问题 |
|------|------|------|
| 张冠李戴 | `// 获取收藏列表` 但代码实际是获取预约列表 | 功能错位 |
| 参数错误 | `@param amount 金额（单位：分）` 但代码用 `元` | 单位/类型错误 |
| 逻辑矛盾 | `// 当用户未登录时...` 但代码判断的是 `isLoggedIn === true` | 条件反了 |
| 残留注释 | 代码已改但注释未更新 | 过时信息 |
| 误导性 | `// 简单处理` 实际有 50 行复杂逻辑 | 掩盖复杂度 |

**严重度分级：**

| 级别 | 说明 | 报告标记 |
|------|------|:------:|
| 🔴 严重 | 注释和代码完全矛盾，会误导开发者 | 必须修复 |
| 🟡 中等 | 注释有歧义或不完整，可能引起困惑 | 建议修复 |
| 🟢 轻微 | 注释可以更精准但不会引起误解 | 建议优化 |

#### 维度三：小白友好度（权重 25%）

> 注释要让**非技术背景的人**也能大致看懂这段代码在做什么

**检查标准：**

1. **禁止纯技术术语堆砌**：
   - ❌ `// 利用 Promise 链式调用实现异步数据获取`
   - ✅ `// 先从云数据库拿数据，失败了就用本地备用数据，保证页面不会白屏`

2. **复杂逻辑必须有白话解释**：
   - ✅ `// 这段代码就像"超市结账"：先扫商品（遍历数据），再算总价（累加），最后打折（应用优惠）`

3. **数字和阈值必须有来源说明**：
   - ❌ `if (points < 200)`
   - ✅ `// 兑换纯色美甲需要 200 积分，积分不够的话提示用户先去签到攒分`

4. **函数注释必须说明"为什么需要"**：
   - ❌ `// 获取用户预约`（这是"做什么"，代码本身就能看出来）
   - ✅ `// 加载用户的所有预约记录，用于"我的预约"页面展示，按时间倒序排列——最新的预约在最上面`

5. **每个文件顶部必须有模块说明**：
   ```
   /**
    * 积分中心页面
    * 
    * 负责：显示积分余额、每日签到、跳转积分商城
    * 为什么需要：让顾客知道自己有多少积分，可以通过签到和消费攒积分
    * 使用场景：顾客点击「我的」→「积分中心」进入
    */
   ```

---

### 检查报告格式

```
══════════════════════════════════
  📋 注释检查报告
  📁 检查文件：N 个
  📅 检查时间：YYYY-MM-DD HH:MM
══════════════════════════════════

📊 总体评分：XX/100 分（等级）

──────────────────────────────────

📁 pages/booking/booking.js
  注释率：12/85 = 14% ⭐ 需改进
  准确性：发现 2 处问题
  友好度：5 处可优化

  🔴 严重问题：
    L42: 注释说"计算总价"，代码实际是筛选可用时段
    → 建议：改为 "// 从所有时段中筛选出未被预约的时间段"

  🟡 中等问题：
    L15: submitBooking 函数缺少 JSDoc 注释
    → 建议：添加 /** 提交预约申请 @param {Object} data 预约信息 @returns {Promise} */
```

### 汇总评分公式

```
总分 = 覆盖率得分 × 40% + 准确性得分 × 35% + 友好度得分 × 25%

覆盖率得分 = MIN(注释率 / 30% × 100, 100)
准确性得分 = 100 - 严重问题数×20 - 中等问题数×10 - 轻微问题数×3
友好度得分 = 100 - 不友好项数×10
```

---

## security-audit

对项目进行全面的安全审计——检查硬编码密钥、数据安全、云开发配置、输入验证等七大类别，并给出修复方案和优先级排序。

### 什么是安全审计？

> 就像给房子做消防检查——不是等火灾发生了再补救，而是提前检查灭火器有没有过期、电线有没有老化、逃生通道有没有堵住。
> 安全审计 = 逐行扫描代码，找出可能被攻击者利用的漏洞，在出事之前修好。

### 使用方式

| 命令 | 功能 |
|------|------|
| `/security-audit` | 全面审计——源码 + 配置 + 云开发 |
| `/security-audit 快速` | 快速扫描——只查 🔴 严重级别（5 分钟） |
| `/security-audit pages/booking/booking.js` | 单文件审计 |
| `/security-audit pages/` | 按目录审计 |

---

### 审计范围

| 范围 | 扫描内容 |
|------|----------|
| 页面源码 | `pages/**/*.js` `pages/**/*.wxml` |
| 工具模块 | `utils/*.js` |
| 全局文件 | `app.js` `app.json` |
| 配置文件 | `project.config.json` |
| 云函数 | `cloudfunctions/**/*.js` |

---

### 七大检查类别一览

| # | 检查类别 | 严重度 | 核心检查内容 |
|:---:|----------|:------:|------------|
| 1 | 硬编码敏感信息 | 🔴 | 密码、API Key、AppSecret、云环境ID是否明文写在代码中 |
| 2 | 云开发安全配置 | 🔴 | 数据库权限设置、云函数是否校验用户身份 |
| 3 | 数据注入风险 | 🔴 | 是否有未过滤的用户输入直接写入数据库 |
| 4 | 敏感数据泄露 | 🟡 | 用户手机号、openid 等隐私数据是否安全处理 |
| 5 | 输入验证缺失 | 🟡 | 金额、备注、预约信息是否有校验 |
| 6 | 权限控制 | 🟡 | 管理员操作是否验证身份、非管理员能否访问后台 |
| 7 | 其他安全隐患 | 🟢~🟡 | console.log 泄露、硬编码 URL、未处理的异常等 |

---

### 严重度定义

| 级别 | 图标 | 含义 | 处理时限 |
|------|:----:|------|:--------:|
| 严重 | 🔴 | 可直接导致数据泄露、用户隐私暴露 | **立即修复**（本次发布前） |
| 中等 | 🟡 | 存在潜在风险，在特定条件下可被利用 | **尽快修复**（本周内） |
| 轻微 | 🟢 | 不良实践，不会直接导致安全事件 | **逐步改进**（下个迭代） |
| 通过 | ⚪ | 该项检查无问题 | — |

---

### 审计报告格式

```
╔══════════════════════════════════════╗
║     🔒 安全审计报告                  ║
║     📁 扫描文件：N 个                ║
╚══════════════════════════════════════╝

📊 总体评分：XX/100 分（🟢安全 / 🟡需改进 / 🟠有风险 / 🔴高危）

🔴 严重：N 项  |  🟡 中等：N 项  |  🟢 轻微：N 项  |  ⚪ 通过：N 项
```

### 汇总评分公式

```
总分 = Σ(各项得分) / 检查项数

安全等级：
  90-100 分 → 🟢 安全
  70-89 分  → 🟡 需改进
  50-69 分  → 🟠 有风险
  < 50 分   → 🔴 高危
```

### 小程序特有安全检查

#### 管理员权限校验
检查所有 admin 页面的 `onLoad` 中是否校验了 `isAdmin`：
```js
// ✅ 安全做法
onLoad() {
  if (!app.globalData.isAdmin) {
    wx.showToast({ title: '无权限访问', icon: 'none' });
    wx.navigateBack();
    return;
  }
  // ... 加载管理数据
}
```

#### 云数据库权限
确认各集合的权限设置合理：
| 集合 | 建议权限 |
|------|----------|
| nail_styles | 所有用户可读，仅管理员可写 |
| bookings | 用户可读自己的，管理员可读写全部 |
| admin_users | 仅管理员可读 |
| points | 用户可读自己的，系统自动写入 |

---

## test

对美甲小程序项目进行功能测试验证。

### 小程序测试说明

> 微信小程序无法使用 Vitest/Jest 等传统单元测试框架进行自动化测试。
> 小程序的测试方式以**人工操作验证**为主，即在微信开发者工具模拟器和真机预览中手动测试。

### 使用方式

| 命令 | 功能 |
|------|------|
| `/test` | 生成完整测试清单 → 逐项指导用户测试 → 记录测试结果 |
| `/test 预约流程` | 只测试指定功能模块 |
| `/test 管理后台` | 只测试管理员相关功能 |
| `/test 积分` | 只测试积分系统 |

---

### 测试清单（全功能覆盖）

#### 模块 1：🏠 首页

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | Banner 是否正常轮播 | 3 张 Banner 自动轮播，可手动滑动 |
| 2 | 公告栏是否滚动 | 公告文字横向滚动显示 |
| 3 | 分类入口是否可点击 | 点击"款式图库"→ 跳转款式图库页；点击"服务价格"→ 跳转服务页 |
| 4 | 热门款式是否显示 | 显示多张热门款式卡片，可点击进入详情 |
| 5 | 新品推荐是否显示 | 显示新品款式卡片 |
| 6 | 活动卡片是否展示 | 显示活动信息卡片 |

#### 模块 2：🖼️ 款式图库

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | 子分类Tab切换 | 点击"纯色/法式/猫眼/渐变/贴片/雕花"Tab 正确筛选 |
| 2 | 瀑布流布局 | 左右两列高度自适应 |
| 3 | 收藏功能 | 点击心形图标 → 变红/取消 → 变白 |
| 4 | 点击进详情 | 点击款式卡片 → 跳转款式详情页 |
| 5 | 搜索功能 | 输入关键词能筛选出匹配的款式 |

#### 模块 3：📅 在线预约

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | 选择服务项目 | 显示 7 项美甲服务，可单选 |
| 2 | 选择技师 | 显示 3 位技师，可单选 |
| 3 | 选择日期 | 显示未来 7 天，已满日期不可选 |
| 4 | 选择时段 | 显示 10:00-20:00 时段，已约时段不可选 |
| 5 | 提交预约 | 弹出确认框 → 确认后跳转成功页 |
| 6 | 预约记录 | 预约成功后，"我的预约"中能看到 |

#### 模块 4：🎨 DIY美甲

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | 肤色选择 | 4种肤色可切换，手模图跟随变化 |
| 2 | 颜色选择 | 7色系可切换，点击色块手指变色 |
| 3 | 全手同色 | 点击"全手同色"一键设置 |
| 4 | 保存作品 | 作品保存到"我的作品集" |
| 5 | 模板选择 | 预设模板可一键套用 |

#### 模块 5：⭐ 积分系统

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | 每日签到 | 点击签到 → 积分+5 → 按钮变灰 |
| 2 | 连续签到 | 第7天额外+15（需实际测试7天或用模拟数据） |
| 3 | 积分商城 | 显示可兑换商品列表 |
| 4 | 兑换商品 | 积分足够 → 兑换成功扣积分；不够 → 提示积分不足 |
| 5 | 积分明细 | 显示所有积分变动记录 |

#### 模块 6：🔧 管理后台

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | 管理员入口 | 管理员微信登录后"我的"页面显示"管理后台"入口 |
| 2 | 非管理员 | 普通用户看不到管理后台入口 |
| 3 | 后台首页 | 显示预约数、待确认数等统计数据 |
| 4 | 款式管理 | 可新增/编辑/删除款式 |
| 5 | 预约管理 | 可查看/确认/取消预约 |
| 6 | 公告管理 | 可发布/编辑/删除公告 |

#### 模块 7：👤 通用功能

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | TabBar 切换 | 5个Tab正常切换，选中态图标正确 |
| 2 | 页面返回 | 左上角返回按钮功能正常 |
| 3 | 门店信息 | 地图显示正确位置，电话可拨打 |
| 4 | 分享功能 | 右上角分享按钮正常，分享卡片内容正确 |

---

### 测试报告格式

```
══════════════════════════════════
  📋 功能测试报告
  📅 测试时间：YYYY-MM-DD HH:MM
  🖥️ 测试环境：模拟器 / 真机
══════════════════════════════════

📊 测试结果汇总

  总测试项：N 项
  ✅ 通过：N 项
  ❌ 失败：N 项
  ⏭️ 跳过：N 项
  通过率：XX%

──────────────────────────────────
❌ 失败详情：

  [模块] 📅 在线预约
  [测试项] 选择技师
  [问题描述] 点击技师卡片后无选中态反馈
  [严重度] 🟡 中等 — 功能正常但用户体验差
  [建议] 在 booking.js 中添加 selectedTechnician 状态的视觉反馈

──────────────────────────────────
📋 未测试项（需要特定条件）：

  ⏭️ 云开发数据加载 — 需要先开通云开发
  ⏭️ 微信支付 — 个体户不支持
  ⏭️ 连续签到7天奖励 — 需要实际签到7天
```

---

### 测试原则

1. 按模块顺序测试，每个模块全部通过再测下一个
2. 发现 Bug 先记录，不要中断测试流程
3. 测试完一个模块后，由 Claude 协助修复发现的 Bug
4. 修复后再复测确认
